<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acorn.elearning.admin.mapper.AdminContentMapper">
    
    <update id="deactivateCurriculumNodesBySubjectId">
        UPDATE curriculum_nodes
        SET is_active = 0,
            updated_at = NOW()
        WHERE subject_id = #{subjectId}
            AND is_active = 1
    </update>

    <update id="deactivateLessonsBySubjectId">
        UPDATE lessons l
        JOIN curriculum_nodes cn
            ON cn.node_id = l.node_id
        SET l.is_active = 0,
            l.updated_at = NOW()
        WHERE cn.subject_id = #{subjectId}
            AND l.is_active = 1
    </update>

    <update id="deactivateProblemsBySubjectId">
        UPDATE practice_problems
        SET is_active = 0,
            updated_at = NOW()
        WHERE subject_id = #{subjectId}
            AND is_active = 1
    </update>

    <update id="cancelActiveExamsBySubjectId">
        UPDATE exam_sessions
        SET status = 'FAILED',
            updated_at = NOW()
        WHERE subject_id = #{subjectId}
            AND status IN ('CREATED', 'READY')
    </update>
</mapper>
