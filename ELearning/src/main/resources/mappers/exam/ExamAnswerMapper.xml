<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acorn.elearning.exam.mapper.ExamAnswerMapper">
    <!-- TODO resultMap 구현 예시입니다.
      <resultMap id="ExamAnswerResultMap" type="com.acorn.elearning.exam.model.ExamAnswer">
          <id property="id" column="id" />
          <result property="..." column="..." />
      </resultMap>
    -->
    <!-- TODO findById 구현 예시입니다.
      <select id="findById" parameterType="long" resultMap="ExamAnswerResultMap">
          SELECT /* DDL column list */
          FROM exam_answers
          WHERE id = #{id}
      </select>
    -->
    <!-- TODO findAll 구현 예시입니다.
      <select id="findAll" resultMap="ExamAnswerResultMap">
          SELECT /* DDL column list */
          FROM exam_answers
          ORDER BY id DESC
      </select>
    -->
    <!-- TODO insert 구현 예시입니다.
      <insert id="insert" parameterType="com.acorn.elearning.exam.model.ExamAnswer" useGeneratedKeys="true" keyProperty="id">
          INSERT INTO exam_answers (/* writable columns */)
          VALUES (/* #{property} values */)
      </insert>
    -->
    <!-- TODO update 구현 예시입니다.
      <update id="update" parameterType="com.acorn.elearning.exam.model.ExamAnswer">
          UPDATE exam_answers
          SET /* changed columns = #{property} */
          WHERE id = #{id}
      </update>
    -->
</mapper>
