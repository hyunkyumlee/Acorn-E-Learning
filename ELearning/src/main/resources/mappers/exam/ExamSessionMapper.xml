<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acorn.elearning.exam.mapper.ExamSessionMapper">
    <!-- TODO resultMap 구현 예시입니다.
      <resultMap id="ExamSessionResultMap" type="com.acorn.elearning.exam.model.ExamSession">
          <id property="id" column="id" />
          <result property="..." column="..." />
      </resultMap>
    -->
    <!-- TODO findById 구현 예시입니다.
      <select id="findById" parameterType="long" resultMap="ExamSessionResultMap">
          SELECT /* DDL column list */
          FROM exam_sessions
          WHERE id = #{id}
      </select>
    -->
    <!-- TODO findAll 구현 예시입니다.
      <select id="findAll" resultMap="ExamSessionResultMap">
          SELECT /* DDL column list */
          FROM exam_sessions
          ORDER BY id DESC
      </select>
    -->
    <!-- TODO insert 구현 예시입니다.
      <insert id="insert" parameterType="com.acorn.elearning.exam.model.ExamSession" useGeneratedKeys="true" keyProperty="id">
          INSERT INTO exam_sessions (/* writable columns */)
          VALUES (/* #{property} values */)
      </insert>
    -->
    <!-- TODO update 구현 예시입니다.
      <update id="update" parameterType="com.acorn.elearning.exam.model.ExamSession">
          UPDATE exam_sessions
          SET /* changed columns = #{property} */
          WHERE id = #{id}
      </update>
    -->
</mapper>
