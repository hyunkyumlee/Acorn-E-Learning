<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acorn.elearning.content.mapper.ContentRecommendationMapper">
    <!-- TODO resultMap 구현 예시입니다.
      <resultMap id="ContentRecommendationResultMap" type="com.acorn.elearning.content.model.ContentRecommendation">
          <id property="id" column="id" />
          <result property="..." column="..." />
      </resultMap>
    -->
    <!-- TODO findById 구현 예시입니다.
      <select id="findById" parameterType="long" resultMap="ContentRecommendationResultMap">
          SELECT /* DDL column list */
          FROM content_recommendations
          WHERE id = #{id}
      </select>
    -->
    <!-- TODO findAll 구현 예시입니다.
      <select id="findAll" resultMap="ContentRecommendationResultMap">
          SELECT /* DDL column list */
          FROM content_recommendations
          ORDER BY id DESC
      </select>
    -->
    <!-- TODO insert 구현 예시입니다.
      <insert id="insert" parameterType="com.acorn.elearning.content.model.ContentRecommendation" useGeneratedKeys="true" keyProperty="id">
          INSERT INTO content_recommendations (/* writable columns */)
          VALUES (/* #{property} values */)
      </insert>
    -->
    <!-- TODO update 구현 예시입니다.
      <update id="update" parameterType="com.acorn.elearning.content.model.ContentRecommendation">
          UPDATE content_recommendations
          SET /* changed columns = #{property} */
          WHERE id = #{id}
      </update>
    -->
</mapper>
