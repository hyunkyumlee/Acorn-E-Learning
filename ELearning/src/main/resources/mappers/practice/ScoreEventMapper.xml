<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acorn.elearning.practice.mapper.ScoreEventMapper">
    <!-- TODO resultMap 구현 예시입니다.
      <resultMap id="ScoreEventResultMap" type="com.acorn.elearning.practice.model.ScoreEvent">
          <id property="id" column="id" />
          <result property="..." column="..." />
      </resultMap>
    -->
    <!-- TODO findById 구현 예시입니다.
      <select id="findById" parameterType="long" resultMap="ScoreEventResultMap">
          SELECT /* DDL column list */
          FROM score_events
          WHERE id = #{id}
      </select>
    -->
    <!-- TODO findAll 구현 예시입니다.
      <select id="findAll" resultMap="ScoreEventResultMap">
          SELECT /* DDL column list */
          FROM score_events
          ORDER BY id DESC
      </select>
    -->
    <!-- TODO insert 구현 예시입니다.
      <insert id="insert" parameterType="com.acorn.elearning.practice.model.ScoreEvent" useGeneratedKeys="true" keyProperty="id">
          INSERT INTO score_events (/* writable columns */)
          VALUES (/* #{property} values */)
      </insert>
    -->
    <!-- TODO update 구현 예시입니다.
      <update id="update" parameterType="com.acorn.elearning.practice.model.ScoreEvent">
          UPDATE score_events
          SET /* changed columns = #{property} */
          WHERE id = #{id}
      </update>
    -->
</mapper>
