<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acorn.elearning.practice.mapper.PracticeSubmissionMapper">
    <!-- TODO resultMap 구현 예시입니다.
      <resultMap id="PracticeSubmissionResultMap" type="com.acorn.elearning.practice.model.PracticeSubmission">
          <id property="id" column="id" />
          <result property="..." column="..." />
      </resultMap>
    -->
    <!-- TODO findById 구현 예시입니다.
      <select id="findById" parameterType="long" resultMap="PracticeSubmissionResultMap">
          SELECT /* DDL column list */
          FROM practice_submissions
          WHERE id = #{id}
      </select>
    -->
    <!-- TODO findAll 구현 예시입니다.
      <select id="findAll" resultMap="PracticeSubmissionResultMap">
          SELECT /* DDL column list */
          FROM practice_submissions
          ORDER BY id DESC
      </select>
    -->
    <!-- TODO insert 구현 예시입니다.
      <insert id="insert" parameterType="com.acorn.elearning.practice.model.PracticeSubmission" useGeneratedKeys="true" keyProperty="id">
          INSERT INTO practice_submissions (/* writable columns */)
          VALUES (/* #{property} values */)
      </insert>
    -->
    <!-- TODO update 구현 예시입니다.
      <update id="update" parameterType="com.acorn.elearning.practice.model.PracticeSubmission">
          UPDATE practice_submissions
          SET /* changed columns = #{property} */
          WHERE id = #{id}
      </update>
    -->
</mapper>
