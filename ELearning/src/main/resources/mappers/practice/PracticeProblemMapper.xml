<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acorn.elearning.practice.mapper.PracticeProblemMapper">
    <!-- TODO resultMap 구현 예시입니다.
      <resultMap id="PracticeProblemResultMap" type="com.acorn.elearning.practice.model.PracticeProblem">
          <id property="id" column="id" />
          <result property="..." column="..." />
      </resultMap>
    -->
    <!-- TODO findById 구현 예시입니다.
      <select id="findById" parameterType="long" resultMap="PracticeProblemResultMap">
          SELECT /* DDL column list */
          FROM practice_problems
          WHERE id = #{id}
      </select>
    -->
    <!-- TODO findAll 구현 예시입니다.
      <select id="findAll" resultMap="PracticeProblemResultMap">
          SELECT /* DDL column list */
          FROM practice_problems
          ORDER BY id DESC
      </select>
    -->
    <!-- TODO insert 구현 예시입니다.
      <insert id="insert" parameterType="com.acorn.elearning.practice.model.PracticeProblem" useGeneratedKeys="true" keyProperty="id">
          INSERT INTO practice_problems (/* writable columns */)
          VALUES (/* #{property} values */)
      </insert>
    -->
    <!-- TODO update 구현 예시입니다.
      <update id="update" parameterType="com.acorn.elearning.practice.model.PracticeProblem">
          UPDATE practice_problems
          SET /* changed columns = #{property} */
          WHERE id = #{id}
      </update>
    -->
</mapper>
