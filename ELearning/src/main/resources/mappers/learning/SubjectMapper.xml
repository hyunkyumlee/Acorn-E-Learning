<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acorn.elearning.learning.mapper.SubjectMapper">

    <resultMap id="SubjectResultMap" type="com.acorn.elearning.learning.model.Subject">
        <id property="subjectId" column="subject_id" />
        <result property="subjectCode" column="subject_code" />
        <result property="subjectName" column="subject_name" />
        <result property="description" column="description" />
        <result property="sortOrder" column="sort_order" />
        <result property="isActive" column="is_active" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
    </resultMap>

    <sql id="columns">
        subject_id, subject_code, subject_name, description, sort_order, is_active, created_at, updated_at
    </sql>

    <select id="findById" parameterType="long" resultMap="SubjectResultMap">
        SELECT <include refid="columns" />
        FROM subjects
        WHERE subject_id = #{id}
    </select>

    <select id="findAll" resultMap="SubjectResultMap">
        SELECT <include refid="columns" />
        FROM subjects
        ORDER BY sort_order ASC, subject_id ASC
    </select>

    <insert id="insert" parameterType="com.acorn.elearning.learning.model.Subject"
            useGeneratedKeys="true" keyProperty="subjectId">
        INSERT INTO subjects (subject_code, subject_name, description, sort_order, is_active)
        VALUES (#{subjectCode}, #{subjectName}, #{description}, #{sortOrder}, #{isActive})
    </insert>

    <update id="update" parameterType="com.acorn.elearning.learning.model.Subject">
        UPDATE subjects
        SET subject_code = #{subjectCode},
            subject_name = #{subjectName},
            description  = #{description},
            sort_order   = #{sortOrder},
            is_active    = #{isActive},
            updated_at   = NOW()
        WHERE subject_id = #{subjectId}
    </update>

</mapper>
