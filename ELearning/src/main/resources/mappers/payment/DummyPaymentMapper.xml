<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acorn.elearning.payment.mapper.DummyPaymentMapper">
    <resultMap id="DummyPaymentResultMap" type="com.acorn.elearning.payment.model.DummyPayment">
        <id property="paymentId" column="payment_id" />
        <result property="orderNo" column="order_no" />
        <result property="userId" column="user_id" />
        <result property="productId" column="product_id" />
        <result property="paymentMethod" column="payment_method" />
        <result property="paymentStatus" column="payment_status" />
        <result property="pgProvider" column="pg_provider" />
        <result property="pgTransactionId" column="pg_transaction_id" />
        <result property="amount" column="amount" />
        <result property="paidAt" column="paid_at" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
    </resultMap>

    <resultMap id="PaymentHistoryItemResultMap" type="com.acorn.elearning.payment.model.PaymentHistoryItem">
        <id property="paymentId" column="payment_id" />
        <result property="orderNo" column="order_no" />
        <result property="userId" column="user_id" />
        <result property="productId" column="product_id" />
        <result property="productCode" column="product_code" />
        <result property="productName" column="product_name" />
        <result property="paymentMethod" column="payment_method" />
        <result property="paymentStatus" column="payment_status" />
        <result property="amount" column="amount" />
        <result property="paidAt" column="paid_at" />
        <result property="paymentCreatedAt" column="payment_created_at" />
        <result property="paymentUpdatedAt" column="payment_updated_at" />
        <result property="grantId" column="grant_id" />
        <result property="grantType" column="grant_type" />
        <result property="grantStatus" column="grant_status" />
        <result property="grantedAt" column="granted_at" />
        <result property="expiresAt" column="expires_at" />
    </resultMap>

    <sql id="DummyPaymentColumns">
        payment_id,
        order_no,
        user_id,
        product_id,
        payment_method,
        payment_status,
        pg_provider,
        pg_transaction_id,
        amount,
        paid_at,
        created_at,
        updated_at
    </sql>

    <select id="findById" parameterType="long" resultMap="DummyPaymentResultMap">
        SELECT
        <include refid="DummyPaymentColumns" />
        FROM dummy_payments
        WHERE payment_id = #{id}
    </select>

    <select id="findByOrderNo" parameterType="string" resultMap="DummyPaymentResultMap">
        SELECT
        <include refid="DummyPaymentColumns" />
        FROM dummy_payments
        WHERE order_no = #{orderNo}
    </select>

    <select id="findByOrderNoForUpdate" parameterType="string" resultMap="DummyPaymentResultMap">
        SELECT
        <include refid="DummyPaymentColumns" />
        FROM dummy_payments
        WHERE order_no = #{orderNo}
        FOR UPDATE
    </select>

    <select id="findByIdAndUserId" resultMap="DummyPaymentResultMap">
        SELECT
        <include refid="DummyPaymentColumns" />
        FROM dummy_payments
        WHERE payment_id = #{paymentId}
          AND user_id = #{userId}
    </select>

    <select id="findAll" resultMap="DummyPaymentResultMap">
        SELECT
        <include refid="DummyPaymentColumns" />
        FROM dummy_payments
        ORDER BY payment_id DESC
    </select>

    <select id="findByUserId" parameterType="long" resultMap="DummyPaymentResultMap">
        SELECT
        <include refid="DummyPaymentColumns" />
        FROM dummy_payments
        WHERE user_id = #{userId}
        ORDER BY payment_id DESC
    </select>

    <select id="findLatestByUserId" parameterType="long" resultMap="DummyPaymentResultMap">
        SELECT
        <include refid="DummyPaymentColumns" />
        FROM dummy_payments
        WHERE user_id = #{userId}
        ORDER BY payment_id DESC
        LIMIT 1
    </select>

    <select id="findHistoryByUserId" resultMap="PaymentHistoryItemResultMap">
        SELECT
            dp.payment_id,
            dp.order_no,
            dp.user_id,
            dp.product_id,
            pp.product_code,
            pp.product_name,
            dp.payment_method,
            dp.payment_status,
            dp.amount,
            dp.paid_at,
            dp.created_at AS payment_created_at,
            dp.updated_at AS payment_updated_at,
            pg.grant_id,
            pg.grant_type,
            pg.status AS grant_status,
            pg.granted_at,
            pg.expires_at
        FROM dummy_payments dp
        LEFT JOIN payment_products pp ON pp.product_id = dp.product_id
        LEFT JOIN premium_grants pg
               ON pg.payment_id = dp.payment_id
              AND pg.user_id = dp.user_id
        WHERE dp.user_id = #{userId}
          AND dp.payment_status = 'PAID'
        ORDER BY dp.paid_at DESC, dp.payment_id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countHistoryByUserId" parameterType="long" resultType="long">
        SELECT COUNT(*)
        FROM dummy_payments
        WHERE user_id = #{userId}
          AND payment_status = 'PAID'
    </select>

    <insert id="insert" parameterType="com.acorn.elearning.payment.model.DummyPayment" useGeneratedKeys="true" keyProperty="paymentId">
        INSERT INTO dummy_payments (
            order_no,
            user_id,
            product_id,
            payment_method,
            payment_status,
            pg_provider,
            pg_transaction_id,
            amount,
            paid_at
        )
        VALUES (
            #{orderNo},
            #{userId},
            #{productId},
            #{paymentMethod},
            #{paymentStatus},
            #{pgProvider},
            #{pgTransactionId},
            #{amount},
            #{paidAt}
        )
    </insert>

    <insert id="insertPending" parameterType="com.acorn.elearning.payment.model.DummyPayment" useGeneratedKeys="true" keyProperty="paymentId">
        INSERT INTO dummy_payments (
            order_no,
            user_id,
            product_id,
            payment_method,
            payment_status,
            pg_provider,
            pg_transaction_id,
            amount
        )
        VALUES (
            #{orderNo},
            #{userId},
            #{productId},
            #{paymentMethod},
            'PENDING',
            #{pgProvider},
            #{pgTransactionId},
            #{amount}
        )
    </insert>

    <insert id="insertPaid" parameterType="com.acorn.elearning.payment.model.DummyPayment" useGeneratedKeys="true" keyProperty="paymentId">
        INSERT INTO dummy_payments (
            order_no,
            user_id,
            product_id,
            payment_method,
            payment_status,
            pg_provider,
            pg_transaction_id,
            amount,
            paid_at
        )
        VALUES (
            #{orderNo},
            #{userId},
            #{productId},
            #{paymentMethod},
            'PAID',
            #{pgProvider},
            #{pgTransactionId},
            #{amount},
            CURRENT_TIMESTAMP
        )
    </insert>

    <update id="markPaid">
        UPDATE dummy_payments
        SET
            payment_status = 'PAID',
            pg_provider = #{pgProvider},
            pg_transaction_id = #{pgTransactionId},
            paid_at = CURRENT_TIMESTAMP,
            updated_at = CURRENT_TIMESTAMP
        WHERE payment_id = #{paymentId}
          AND payment_status = 'PENDING'
    </update>

    <update id="markFailed">
        UPDATE dummy_payments
        SET
            payment_status = 'FAILED',
            updated_at = CURRENT_TIMESTAMP
        WHERE payment_id = #{paymentId}
          AND payment_status = 'PENDING'
    </update>

    <update id="markCanceled">
        UPDATE dummy_payments
        SET
            payment_status = 'CANCELED',
            updated_at = CURRENT_TIMESTAMP
        WHERE payment_id = #{paymentId}
          AND payment_status = 'PENDING'
    </update>

    <update id="update" parameterType="com.acorn.elearning.payment.model.DummyPayment">
        UPDATE dummy_payments
        SET
            order_no = #{orderNo},
            user_id = #{userId},
            product_id = #{productId},
            payment_method = #{paymentMethod},
            payment_status = #{paymentStatus},
            pg_provider = #{pgProvider},
            pg_transaction_id = #{pgTransactionId},
            amount = #{amount},
            paid_at = #{paidAt},
            updated_at = CURRENT_TIMESTAMP
        WHERE payment_id = #{paymentId}
    </update>
</mapper>
