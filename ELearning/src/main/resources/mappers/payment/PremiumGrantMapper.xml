<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acorn.elearning.payment.mapper.PremiumGrantMapper">
    <!-- TODO resultMap 구현 예시입니다.
      <resultMap id="PremiumGrantResultMap" type="com.acorn.elearning.payment.model.PremiumGrant">
          <id property="id" column="id" />
          <result property="..." column="..." />
      </resultMap>
    -->
    <!-- TODO findById 구현 예시입니다.
      <select id="findById" parameterType="long" resultMap="PremiumGrantResultMap">
          SELECT /* DDL column list */
          FROM premium_grants
          WHERE id = #{id}
      </select>
    -->
    <!-- TODO findAll 구현 예시입니다.
      <select id="findAll" resultMap="PremiumGrantResultMap">
          SELECT /* DDL column list */
          FROM premium_grants
          ORDER BY id DESC
      </select>
    -->
    <!-- TODO insert 구현 예시입니다.
      <insert id="insert" parameterType="com.acorn.elearning.payment.model.PremiumGrant" useGeneratedKeys="true" keyProperty="id">
          INSERT INTO premium_grants (/* writable columns */)
          VALUES (/* #{property} values */)
      </insert>
    -->
    <!-- TODO update 구현 예시입니다.
      <update id="update" parameterType="com.acorn.elearning.payment.model.PremiumGrant">
          UPDATE premium_grants
          SET /* changed columns = #{property} */
          WHERE id = #{id}
      </update>
    -->
</mapper>
